<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.latinodistribuidora">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LatinoDistribuidora">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Clientes" />
        <activity android:name=".Registrar_Usuario" />
        <activity android:name=".Listar_Usuario"/>
        <activity android:name=".Editar_Usuario"/>
        <activity android:name=".MenuPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>